[build]
  build = "npm run build"
  publish = "dist"

# Password-protected route
[[redirects]]
  from = "/protected/*"
  to = "/protected/:splat"
  status = 200
  force = true
  headers = { Authorization = "Basic" }

# SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
